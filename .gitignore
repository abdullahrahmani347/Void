# dependencies / tooling (z-ai-web-dev-sdk is the AI backend optional dep)
node_modules/
eslint-cache/

# secrets — never commit keys
.env
.env.*
!.env.example
.z-ai-config

# logs / OS junk
*.log
.DS_Store
Thumbs.db

# local dev artifacts
void-server.log
