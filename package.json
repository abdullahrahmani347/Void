{
  "name": "void-streaming",
  "version": "1.0.0",
  "private": true,
  "description": "VOID — Netflix-style streaming catalog UI powered by TMDB. Vanilla JS, zero runtime dependencies.",
  "scripts": {
    "start": "node dev-server.js 3000",
    "lint": "eslint .",
    "check": "node --check app.js && node --check sw.js && node --check dev-server.js && node --check netlify/functions/tmdb.js && node --check netlify/functions/ai.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-plugin-import": "^2.29.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
